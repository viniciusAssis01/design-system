import { Meta } from "@storybook/addon-docs";
import { TokensGrid } from "../../components/TokensGrid.tsx";
import { space } from "@vns-ignite-ui/tokens";

<Meta title="Tokens/Space" />

# Spaces

Esses sao os espaçamentos utilizados no Ignite UI

{/* lá no componente TokensGrid falamos q iamos receber uma prop chamada tokens export function TokensGrid({ tokens }:...  então: */}

<TokensGrid tokens={space} hasRemValue />
